{
  "$comment": "TEMPLATE FILL-IN — Jev semantic guards (jev-semantic-guards). rails.<review|prose>.mode: off | shadow | jev is the kill-switch AND the promotion lever: one reviewed line. off = today's regex-only behaviour. shadow = the regex decides, Jev is asked and both are logged to .jev/decisions.jsonl — it REQUIRES shadowExpires (YYYY-MM-DD, ≤21 days out) and CI fails once it passes. jev = Jev decides; the regex decides only when Jev cannot look (no key, 429, timeout) or is uncertain. No TYPESAFE_API_KEY, or egress:false, behaves exactly as off. Bump model only after `node scripts/jev-eval.mjs --live`.",
  "model": "jev-1.13.0",
  "egress": true,
  "rails": {
    "review": { "mode": "off", "thresholds": { "real": 0.85, "notReal": 0.15 }, "shadowExpires": null },
    "prose": { "mode": "off", "thresholds": { "claim": 0.5 }, "shadowExpires": null }
  }
}

# Spawned-project defaults — extend, do not replace.
.DS_Store
node_modules/
.env.local
# Local overlay for reporting.config.json (a chat id in a public repo) — see scripts/lib/reporting-config.mjs
reporting.config.local.json

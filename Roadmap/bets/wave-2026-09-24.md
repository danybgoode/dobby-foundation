# Wave 2026-09-24 — Golden Frijoles, wave 2: configure

Wave 1 of [`golden-frijoles-plugin`](../09-platform-infra/golden-frijoles-plugin/README.md) shipped 2026-09-23 (the public
plugin, `@golden-frijoles/kit` on npm, the install prompt). The product owner bet wave 2 on 2026-09-24, after the last
wave-1 check (medusa-bonsai loads the renamed plugin) passed.

| Bet | Appetite | Displaced (the opportunity cost) |
|---|---|---|
| **golden-frijoles-plugin wave 2**: one `golden-frijoles.config.json` + one loader (S4); setup, `gf config`, doctor, Jev egress as an explicit choice (S5). Folds in kit 0.3.1 hardening from review of medusa#195 (`projectAsset` containment, an explicit `scriptsDir`) because it touches the same seam | **M** (one wave of the L epic) | The two seeds from the 2026-09-23 portfolio pass: `think-skills` (Seed 5, the pre-planning chain) and `verify-module` (after the spike). Both stay `raw`; neither is scheduled until this wave closes |
